import { SectionName } from './section-name.enum';
import { Url } from 'url';
export class Dashboard {
    image:string;
    description:string;
    button:string;
    color:string;
    txtcolor:string;
    section:SectionName;
}
